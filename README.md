# فرم آتیه - سیستم مدیریت

## ⚙️ تنظیمات اولیه

### 1. تنظیم رمز عبور ادمین

فایل `config.js` را باز کنید و رمز عبور ادمین را تغییر دهید:

```javascript
const ADMIN_PASSWORD = 'admin123'; // ⚠️ این رمز را تغییر دهید!
```

⚠️ **هشدار مهم**: حتماً یک رمز عبور قوی و منحصر به فرد انتخاب کنید!

## 🚀 Deploy در Vercel

### روش ساده (از طریق وب‌سایت):

1. به https://vercel.com بروید
2. با GitHub خود وارد شوید  
3. روی **"New Project"** کلیک کنید
4. Repository خود را انتخاب کنید
5. روی **"Deploy"** کلیک کنید
6. ✅ تمام! آدرس شما آماده است (مثل: `your-project.vercel.app`)

### روش CLI:

```bash
npm i -g vercel
vercel
```

## 🔐 نحوه استفاده

### برای کاربران عادی:
1. وارد سایت می‌شوند
2. نام خود را در بخش **"ثبت نام شخص"** ثبت می‌کنند
3. فرم‌های مختلف (پرداخت، درمان، دکتر) را پر می‌کنند
4. داده‌ها در localStorage مرورگرشان ذخیره می‌شوند
5. ✅ **بخش "پنل ادمین" برای آنها نمایش داده نمی‌شود** (کاملاً پنهان است)

### برای ادمین (شما):
1. وارد سایت می‌شوید
2. روی بخش **"🔐 پنل ادمین"** در منو کلیک می‌کنید
3. رمز عبور ادمین را وارد می‌کنید (همان رمزی که در `config.js` تنظیم کرده‌اید)
4. ✅ وارد پنل ادمین می‌شوید
5. می‌توانید:
   - آمار کلی را مشاهده کنید
   - لیست تمام داده‌ها را ببینید
   - داده‌ها را Export کنید (دانلود JSON)
   - داده‌ها را پاک کنید (در صورت نیاز)
   - از پنل ادمین خارج شوید

## ⚠️ محدودیت‌های راه حل فعلی

**مهم**: این راه حل ساده است اما محدودیت دارد:

- ✅ بخش پنل ادمین فقط برای شما قابل مشاهده است (با رمز عبور)
- ✅ کاربران عادی هرگز بخش ادمین را نمی‌بینند
- ⚠️ داده‌ها در localStorage مرورگر هر کاربر ذخیره می‌شوند
- ⚠️ شما فقط داده‌های localStorage مرورگر **خودتان** را می‌بینید
- ⚠️ برای مشاهده داده‌های کاربران، باید از آنها بخواهید:
  1. وارد پنل ادمین شوند (باید رمز عبور را به آنها بدهید - توصیه نمی‌شود!)
  2. دکمه "📥 دانلود تمام داده‌ها (JSON)" را بزنند
  3. فایل JSON را برای شما بفرستند

## 💡 راه حل بهتر (اگر می‌خواهید همه داده‌ها را خودتان ببینید)

اگر می‌خواهید **مستقیماً** به همه داده‌های کاربران دسترسی داشته باشید (بدون نیاز به ارسال فایل)، باید از یک Backend مثل:
- **Supabase** (رایگان، ساده)
- **Firebase** (رایگان)
- **Custom Backend**

استفاده کنید. در این صورت، همه داده‌ها در یک دیتابیس مشترک ذخیره می‌شوند و شما می‌توانید همه را ببینید.

## 📝 نکات مهم

- 🔒 بخش پنل ادمین فقط برای کسانی که رمز عبور درست را وارد کنند نمایش داده می‌شود
- 👀 کاربران عادی هیچ وقت بخش ادمین را نمی‌بینند (کاملاً پنهان است)
- 💾 رمز عبور ادمین در localStorage ذخیره می‌شود (تا زمان پاک کردن cache باقی می‌ماند)
- 🚪 می‌توانید با دکمه "خروج از پنل ادمین" از حساب ادمین خارج شوید

## 🎯 خلاصه

این یک راه حل **ساده و سریع** است که:
- ✅ بدون نیاز به backend کار می‌کند
- ✅ بخش ادمین را محافظت می‌کند
- ✅ کاربران عادی بخش ادمین را نمی‌بینند
- ⚠️ برای مشاهده داده‌های کاربران نیاز به راه حل backend دارید

---

**نکته**: اگر تعداد کاربران شما زیاد است یا می‌خواهید همه داده‌ها را مستقیماً ببینید، حتماً از یک Backend استفاده کنید.
